# Pydantic + DB model stubs: Agent Identity, Grant, Approval Request,
# Audit Log Entry — shapes defined in PRD.md §6 (Data Model).
# Registry-side models store identity/routing/audit metadata only,
# never capsule content (PRD.md §4.2). TODO: define stub classes once
# implementation begins.
