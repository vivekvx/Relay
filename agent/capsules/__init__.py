# Capsule loading, FTS5 search, and frontmatter parsing (`shareable`,
# `shareable-with` metadata). Per PRD.md §4.2 "Consent-scoped memory /
# capsule store" and §6 (Capsule data model). Local markdown +
# SQLite/FTS5 only — never synced to the hosted registry.
# TODO: implementation.
